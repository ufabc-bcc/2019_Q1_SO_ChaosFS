#pragma once

#include <stdbool.h>
#include <string.h>

// extern bool cmp_nomes_rs(const char* a, const char* b);

// extern char* last_name(const char* a);

bool compara_nome (const char *a, const char *b);
